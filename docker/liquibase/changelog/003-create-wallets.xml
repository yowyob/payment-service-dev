<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="003-create-wallets" author="yowyob">
        <sql>
            CREATE TABLE "yy-pay-wallets" (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                user_id UUID NOT NULL REFERENCES "yy-pay-users"(id),
                balance DECIMAL(19,4) NOT NULL DEFAULT 0.0,
                status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
                created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
                updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
            );
            CREATE INDEX idx_yy_pay_wallets_user_id ON "yy-pay-wallets"(user_id);
            CREATE INDEX idx_yy_pay_wallets_status ON "yy-pay-wallets"(status);
        </sql>
    </changeSet>

</databaseChangeLog>
